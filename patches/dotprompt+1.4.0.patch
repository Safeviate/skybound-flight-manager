# This is an autogenerated file.
# It was created by patch-package.
# You should not modify it by hand.
#
# Similarly, while this file is under version control,
# you should not create additional patches for this package
# unless you are confident that the patch process will apply them
# in the correct order.

patch-package
--- a/node_modules/dotprompt/dist/index.js
+++ b/node_modules/dotprompt/dist/index.js
@@ -37,7 +37,7 @@
     return result.replace(/\{\{\{/g, "{{").replace(/\}\}\}/g, "}}");
 };
 const compile = (template, options) => {
-    const Handlebars = require("handlebars/dist/cjs/handlebars.js");
+    const Handlebars = require("handlebars");
     return Handlebars.compile(template, options);
 };
 const parsePart = (text) => {
